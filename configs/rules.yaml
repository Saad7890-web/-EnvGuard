version: 1

checks:
  - name: docker
    type: docker
    fix: fix_docker

  - name: postgres_port_5432
    type: port
    port: 5432
    fix: kill_port

  - name: disk_root
    type: disk
    path: /
    threshold_percent: 90
    fix: clean_temp
